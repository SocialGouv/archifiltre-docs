name: Print old keys

on:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  print:
    name: Print old keys
    runs-on: ubuntu-latest
    steps:
      - name: print keys
        run: |
          echo "${{secrets.APPLE_ID}}" | base64
          echo "${{secrets.APPLE_ID_PASSWORD}}" | base64
